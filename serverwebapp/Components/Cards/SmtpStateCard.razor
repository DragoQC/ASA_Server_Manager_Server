<BaseCard Title="SMTP" Subtitle="Outbound email" Stat="@State" StatHint="settings" Status="@Status" />

@code {
    [Parameter]
    public string State { get; set; } = "Unknown";

    [Parameter]
    public string? Status { get; set; }
}
